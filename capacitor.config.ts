import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dreambridgehq.pocketmoney',
  appName: 'Pocket Money Ads',
  webDir: 'dist'
};

export default config;
